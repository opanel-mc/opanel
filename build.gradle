plugins {
    /* This plugin is workaround for https://github.com/neoforged/NeoGradle/issues/282 */
    id 'org.ipnmod.neosync' version '1.2.0'
    id 'fabric-loom' version '1.13-SNAPSHOT' apply false
    id 'java'
}

allprojects {
    apply plugin: 'java'

    group = project.group
    version = project.version

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(14)
        }
    }

    tasks.withType(JavaCompile).configureEach {
        options.encoding = 'UTF-8'
    }
}
